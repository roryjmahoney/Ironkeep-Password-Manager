{
  "name": "ironkeep",
  "version": "0.11.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Offline-first, zero-knowledge password manager for Android and browsers.",
  "workspaces": [
    "shared",
    "shared/extension-ui",
    "extensions/chromium",
    "extensions/firefox"
  ],
  "scripts": {
    "build": "node scripts/verify-legal-documents.mjs && npm run build --workspaces --if-present && node scripts/verify-extension-content-scripts.mjs",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
